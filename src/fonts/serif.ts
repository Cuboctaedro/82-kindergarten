import { Comfortaa } from 'next/font/google';

export const sourceSerif = Comfortaa({
    subsets: ['latin', 'greek'],
    display: 'swap',
    variable: '--font-source-serif',
    weight: '700',
});
